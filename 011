public class Solution {
    public int NumberOf1(int n) {
        int count =0;
        while(n!=0){
            n=n&(n-1);      //把n最右边的1去掉
            count++;

        }
        return count;
    }
}
